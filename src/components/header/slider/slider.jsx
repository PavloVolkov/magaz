import React from 'react';
import PropTypes from 'prop-types';
import styles from "./slider.module.css"

const slider = props => {
    return (
        <>
            <div className={styles.slider}>slider here</div>
        </>
    );
};

slider.propTypes = {
    
};

export default slider;